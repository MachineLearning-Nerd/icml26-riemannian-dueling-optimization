# Source and non-circularity audit

Primary HTML SHA-256:
`1b20e2af562744080126d140c55b72c92658e355d8b93086c0e2908f762fb323`.
Relevant anchors are Theorems 3.6–3.7 (`#S3.Thmtheorem6`,
`#S3.Thmtheorem7`), Algorithm 2 and Theorem 3.10 (`#alg2`,
`#S3.Thmtheorem10`), and Algorithm 3 and Theorem 4.1 (`#alg3`,
`#S4.Thmtheorem1`).

The RDNGD horizon grid is fixed before outcomes are observed. Six calibration
seeds select the first successful horizon; 20 disjoint validation seeds
estimate a 95% upper endpoint. The selected resource is therefore not copied
from the theorem's complexity formula.

The RDFW horizons are also fixed independently. Its batch size and step are
the named algorithm's published schedules, while error-versus-horizon is
measured rather than assumed.
