# Limitations

- Finite sweeps corroborate but do not prove universal upper bounds. They must
  be combined with the independent theorem certificates.
- The tested objectives are clean-room assumption-satisfying instances, not
  worst-case lower-bound constructions.
- RRDNGD uses the proof-consistent schedule; the sibling tests the literal
  printed schedule separately.
